version: '3.8'

services:
  tuvi-app:
    build:
      context: .
      dockerfile: apps/web/Dockerfile.prod
    container_name: tuvi-app
    ports:
      - "8950:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always
